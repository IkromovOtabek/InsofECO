import React from 'react';
import { ErpHome } from '@/features/erp/screens';

/** Ish boshqaruvchi — bosh ekran: ochiq topshiriqlar va bugungi ish (`/api/mobile/home`). */
export default function Home() {
  return <ErpHome />;
}
